package com.guga.asunaanimes.domain.model

/**
 * Browse catalogs available on the Search tab when the user is not typing a query.
 */
enum class AnimeBrowseMode {
    TOP,
    SEASONAL,
    RECOMMENDATIONS
}
